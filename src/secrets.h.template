/*
 * secrets.h — Copy this file to secrets.h and fill in real values.
 * secrets.h is git-ignored (see .gitignore).
 *
 * NEVER commit real keys. NEVER paste tokens in Telegram.
 */
#ifndef SECRETS_H
#define SECRETS_H

// Per-device API key issued by the school-attendance backend at
// device registration time. Rotate via Cloud Function `rotateDeviceKey`.
#define DEVICE_API_KEY  "REPLACE_ME_AT_FIRST_BOOT"

// Optional: device-level HMAC secret for signing payloads (defence in depth)
#define DEVICE_HMAC_KEY "REPLACE_ME_AT_FIRST_BOOT"

// Optional: backend CA cert fingerprint for pin-and-skip verification.
// Leave empty to use system trust store (fine for prototype).
#define BACKEND_CA_FP   ""

#endif // SECRETS_H
